name: torchenv
channels:
    - pytorch
    - conda-forge
    - nodefaults
    - anaconda
dependencies:
    - python=*.*
    - numpy=*.*
    - matplotlib=*.*
    - colorcet=*.*
    - pytorch=*.*
    - torchvision=*.*
    - jupyterlab=*.*
    - pytest=*.*
    - ipywidgets=*.*
    - cudatoolkit=*.*
    - ipykernel=*.*
