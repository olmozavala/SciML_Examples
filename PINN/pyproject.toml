[project]
name = "pinn-dash"
version = "0.1.0"
description = "PINN training and analysis Dash interface"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy",
    "dash>=2.14",
    "dash-bootstrap-components",
    "plotly>=5.0",
]
